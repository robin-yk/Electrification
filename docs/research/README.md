# Research archives

Every directory here is one executed batch, preserved as it ran. This file says
what each one is, which generator produced it, and which of them combine into a
result you can cite. Read it before opening any single archive: several of them
are stopped runs kept as evidence, and two of them are supersets of others.

A directory is not a conclusion. The conclusions live in the combined reports
named below, and the boundaries live in the run cards under
`tools/openmkm_dynamic/`.

## Three model families, and they do not mix

The archives divide by reactor closure, and results never cross a closure
boundary. A number from one family is not comparable to a number from another,
whatever the species and conditions look like.

| Family | Closure | Purpose |
|---|---|---|
| **A. Constant-temperature map** | constant pressure, prescribed constant T, mass flow reset to reactor mass over τ each step, τ is an input | how yield depends on temperature and residence time |
| **B. Fixed-volume composition sweep** | fixed geometric volume, fixed 50 sccm standard feed, pressure controller near 1 atm, τ is an output | how yield depends on feed composition at one temperature |
| **C. Pulsed (RPH)** | prescribed periodic T(t), integrated to a periodic state | what a temperature waveform does that a steady state does not |

Family A carries residence time as a controlled variable and cannot say anything
about a fixed-flow device. Family B holds one temperature and one flow and
cannot say anything about the temperature axis. Family C is a different question
again. None of the three carries a heater energy balance, so none of them
supports an energy-per-product claim, and none includes a soot model.

## A. Constant-temperature maps

Two feeds on one identical 69-condition (T, τ) grid, AramcoMech 2.0 throughout.

| Archive | Feed | Points | Note |
|---|---|---|---|
| `aramco-cjh-2026-09-07` | CH4/CO2 | 9 | the first batch, 3 by 3 |
| `cjh-refine-01-2026-09-07` | CH4/CO2 | 12 | gap filling |
| `cjh-refine-02-2026-09-07` | CH4/CO2 | 0 | **stopped**, comparison bookkeeping bug; kept as evidence |
| `cjh-refine-02-resume-2026-09-07` | CH4/CO2 | 12 | the resume that completed it |
| `cjh-refine-03/04/05-2026-09-07` | CH4/CO2 | 12 each | short-residence extension to 3 µs |
| `h2o-cjh-2026-09-07` | CH4/H2O | 33 | the grid as it then stood |
| `h2o-cjh-extend-2026-09-07` | CH4/H2O | 36 | the 36 the reference map had added |

**Combined reports.** `cjh-refine-05-2026-09-07/REPORT.md` is the 69-point
CH4/CO2 map; `h2o-cjh-extend-2026-09-07/REPORT.md` is the 69-point CH4/H2O map.
Both are unions of the summaries above, regenerated by
`summarize_cjh_map.py`. Cite those two, not the individual batches.

**What they establish.** Both feeds put an interior acetylene maximum at 1 ms:
21.55 % carbon yield at 1750 °C with CO2, 42.12 % at 1800 °C with steam. On the
methane-carbon basis the two share, 43.10 against 42.12 %, so most of the
apparent doubling is the denominator. Methane conversion is lower with steam at
every hot condition. Benzene dominates at moderate severity in both.

**Denominator warning.** Carbon yields are per total inlet carbon. That is all
methane with steam and half CO2 with CO2. The two are one scale only after
multiplying the CO2 figures by two.

Generators: `run_aramco_cjh_baseline.py`, `run_cjh_refine_02.py`,
`run_h2o_cjh_map.py`. Cards: `ARAMCO-CJH-RUN-CARD.md`,
`CJH-REFINE-01` through `05`, `H2O-CJH-RUN-CARD.md`.

## B. Fixed-volume composition sweeps

One temperature, 1750 °C, one reactor volume, one standard feed rate, seven
methane mole fractions from 0.2 to 0.8.

| Archive | Feed | Cases | Note |
|---|---|---|---|
| `cjh-feed-ratio-01-2026-09-07` | CH4/CO2 | 1 | **stopped** at the anchor, local timeout |
| `cjh-feed-ratio-01-remote-2026-09-07` | CH4/CO2 | 1 | **stopped**, stationarity gate failed |
| `cjh-small-volume-diagnostic-2026-09-07` | CH4/CO2 | 1 | the diagnosis: absolute tolerance must scale with volume |
| `cjh-feed-ratio-02-2026-09-07` | CH4/CO2 | 7 | completed, five compositions plus two anchors |
| `cjh-feed-ratio-03-2026-09-07` | CH4/CO2 | 2 | the 0.4 and 0.6 refinement |
| `h2o-feed-ratio-2026-09-07` | CH4/H2O | 10 | completed, all seven compositions in one batch |

**Combined reports.** `cjh-feed-ratio-02-2026-09-07/REPORT.md` plus the two
refinement points; `h2o-feed-ratio-2026-09-07/REPORT.md` for steam.

**What they establish.** Composition moves acetylene harder than anything else
measured here. On each feed's own carbon basis, CO2 runs 4.67 to 41.81 % from
x = 0.2 to 0.8 and steam runs 21.23 to 51.80 %, which makes steam look far ahead.
Renormalized to methane carbon the order reverses and the gap nearly closes: CO2
runs 23.34 to 52.26 % and is ahead of steam at every composition, by 2.11 points
at x = 0.2 narrowing monotonically to 0.46 at x = 0.8. This is the same
denominator effect the constant-temperature maps show, seen along a second axis.
Methane conversion runs 79.6 to 85.3 % with CO2 and a nearly flat 76.0 to 77.9 %
with steam, so CO2 again reaches a larger extent of reaction.

**Cross-family warning.** These are 1750 °C at about 1 ms, which is close to the
family A optimum, but the closures differ and the numbers are not
interchangeable. The steam batch's `co2-anchor` case exists to prove the point
in the other direction: it reproduces family B's archived CH4/CO2 1:1 result to
2.19×10⁻⁷ per species, which is what licenses comparing the two sweeps.

Generators: `run_cjh_feed_ratio_01.py`, `run_h2o_feed_ratio.py`. Cards:
`CJH-FEED-RATIO-01`, `CJH-FEED-RATIO-03`, `H2O-FEED-RATIO-RUN-CARD.md`,
`CJH-SMALL-VOLUME-DIAGNOSTIC.md`.

## C. Pulsed runs

| Archive | Contents | Note |
|---|---|---|
| `rph-2026-09-06` | 11 of 12 paired GRI/Aramco outputs, plus early-stop validation and the operations design | one output missing: 8300048 Aramco refined timed out and was not checkpointed |
| `rph-fixed-gate-01-2026-09-07` | fixed-volume RPH entry gate | |
| `rph-fixed-pulse-01-2026-09-07` | 0 reacting integrations | **stopped** at the inert pressure gate |
| `rph-implementation-reconciliation-2026-09-07.md` | a file, not a batch | read-only survey of 12 branch tips plus isolated algebraic state-update tests; no integration was run |

**What `rph-2026-09-06` establishes.** Changing mechanism changes both magnitude
and order. Acetylene falls 62.30 to 49.43 % on 6300012, and the residence-time
ranking of 6300012 against 6000015 reverses between GRI and Aramco. This is the
direct evidence that no optimum, ranking or validation status transfers across a
mechanism change.

## Supporting and stopped batches

| Archive | What it is |
|---|---|
| `c2co-pilot-2026-09-07` | the prescribed-temperature C2/CO pilot, 8 integrations; P1 passed paired grid checks, P2–P4 are provisional base-grid results |
| `cjh-tolerance-01-2026-09-07` | explicit integrator-tolerance pilot, four comparisons passed |
| `c2co-campaign-2026-09-07` | not results: the budget ledger and the continuation order |

## Reading any archive

Each directory holds `data/` with one raw solver output, parameter record,
metrics file and log per case; `manifest.json` with the commit and the solver and
mechanism SHA256; `status.json` with `completed` or `stopped` and the reason;
`gates.json` where gates ran; and `checksums.json`. Verify an archive with

```bash
python tools/openmkm_dynamic/archive_checksums.py docs/research/<archive> --verify
```

A `status.json` reading `stopped` means the batch did not finish. Its completed
cases are real and its incomplete ones were never written; nothing is
interpolated and no yield is fabricated for a missing case.

## What none of this supports

- **Energy per unit product.** No archive here carries a heater energy balance.
- **Soot or carbon deposition.** AramcoMech 2.0 stops at C8 with benzene as its
  largest ring and no PAH growth. Benzene is where the calculation stops, not
  where the carbon stops. Equilibrium with graphite is not a substitute above
  about 1200 K, where it returns zero solid carbon for these feeds.
- **Experimental validation.** No condition in any archive has been reproduced
  against measured product data for these feeds and conditions.
- **Robustness beyond the recorded gates.** Initial-state dependence is untested
  everywhere; integrator-tolerance dependence is tested only in
  `cjh-tolerance-01` and at the anchors of family B.
